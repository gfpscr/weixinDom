<view class="tabs">
  <view class="tabs_title">
    <view class="tab_title {{item.isActive?'tab_active':''}}" wx:for="{{tabs}}" wx:key="id" bindtap="handleItemTap" data-index="{{item.id}}">{{item.value}}</view>
  </view>
  <view class="tabs_content">
    <!-- <block>
      <view class="goods_content">
        <goods wx:for="{{goodsList}}" wx:key="this" commodity="{{item}}"></goods>
      </view>
    </block> -->
  </view>
</view>